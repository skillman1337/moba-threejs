// src/config/lightConfig.js
import { DirectionalLight } from 'three';

// Define and export directional lights
export const directionalLight0 = new DirectionalLight('#FFFFFF', 25);
directionalLight0.position.set(0, 50, 0);

export const directionalLight1 = new DirectionalLight('#0000B2', 138);
directionalLight1.position.set(0, 50, 0);

export const directionalLight2 = new DirectionalLight('#00293A', 272);
directionalLight2.position.set(0, 50, 0);
